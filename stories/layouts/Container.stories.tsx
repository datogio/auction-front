import { Container as ContainerLayout } from '../../components/layouts';

export default {
  title: 'Layouts/Container',
  component: ContainerLayout,
};

export const Container = () => <ContainerLayout />;
