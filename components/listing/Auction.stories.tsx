import { Auction as AuctionComponent } from '../../components';

const story = {
  title: 'Components/listing/Auction',
  component: AuctionComponent,
};

export const Auction = () => <AuctionComponent />;

export default story;
